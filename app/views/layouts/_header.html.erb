<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="navbar-header">
    <%= link_to t('site_name'), root_path, class: 'navbar-brand' %>
  </div>
  <!-- /.navbar-header -->
  <ul class="nav navbar-top-links navbar-left">
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        درباره‌ی شرکت <%= icon "caret-down" %>
      </a>
      <ul class="dropdown-menu">
        <li>
          <%= link_to t('products'), products_path %>
        </li>
        <li>
          <%= link_to t('gallery'), gallery_path %>
        </li>
        <li>
          <%= link_to t('contactus'), contact_path %>
        </li>
        <li>
          <%= link_to t('aboutus'), about_path %>
        </li>
      </ul>
    </li>
    <% if current_user %>
      <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#">
          <%= current_user.username %> <%= icon "caret-down" %> 
        </a>
        <ul class="dropdown-menu dropdown-user">
          <li>
            <%= link_to icon(:user, t('user_profile')), me_path %>
          </li>
          <li>
            <%= link_to icon("sign-out", t('logout')), destroy_user_session_path, method: :delete %>
          </li>
        </ul>
      </li>
    <% else %>
      <li>
        <%= link_to t('login'), new_user_session_path %>
      </li>
    <% end %>
  </ul>
  <!-- /.navbar-top-links -->
</nav>